You are modifying the GitHub repository `hiyroscript/seren`.

Use `hiyroscript/ponu` only as a visual/reference repository. Do not modify Ponu. The actual implementation belongs entirely in Seren.

This is a careful gameplay-system refactor, not a cosmetic text replacement. Before editing anything, inspect the current latest Seren source and trace every reference related to the airborne launch mechanic, gameplay status effects, temporary immunity, finish-line protection, HUD status presentation, rival rendering, local split-screen rendering, AI decisions, input mappings, localization, documentation, and tests. Do not assume line numbers remain unchanged. Preserve unrelated gameplay.

# Core objective

Completely remove Seren’s airborne **launch mechanic**: the mechanic where a racer brakes, winds up, leaves the road, rises into the air, gains speed/height based on charge, becomes separated from grounded contact, and later lands.

At the same time, replace the game’s current “effects” terminology and presentation with a proper **Conditions** system.

The resulting active Conditions are exactly:

| ID             | Display name | Type   | Color                                                            | Icon concept                                          |
| -------------- | ------------ | ------ | ---------------------------------------------------------------- | ----------------------------------------------------- |
| `boosted`      | Boosted      | Buff   | `#FF9A4A`                                                        | forward double-chevron or lightning/acceleration mark |
| `invulnerable` | Invulnerable | Buff   | `#FFD86B`                                                        | shield                                                |
| `slowed`       | Slowed       | Debuff | `#8A9099`                                                        | downward speed arrow / reduced-speed mark             |
| `obscured`     | Obscured     | Debuff | `#B07A4A`                                                        | crossed/covered eye or visibility-obscured mark       |
| `skidded`      | Skidded      | Debuff | preserve the existing dark Slippery family, preferably `#0B0B0C` | curved skid/tire marks                                |

There must be no `Launched` condition.

There must be no `Winner` condition.

A racer crossing the finish line must still become permanently out of play for the remainder of that race, with all existing result/placement/run-out behavior preserved. This finish protection is a race lifecycle state, not a Condition, and it must never be presented as `Winner`, `Invulnerable`, or any other condition.

# Critical distinction: do not remove the rear-contact speed shove

Seren currently uses confusing launch-related names for two different mechanics.

The airborne launch mechanic must be deleted.

However, rear-ending another car currently gives the victim a short forward shove using identifiers such as `LAUNCH_TIME`, `LAUNCH_BOOST`, `G.launchT`, and the rival `launch` field. That shove is not the airborne mechanic. Preserve its exact gameplay behavior and tuning, but rename it so the source no longer conflates it with the removed launch system.

Use a clear term such as `SHUNT_TIME`, `SHUNT_BOOST`, `G.shuntT`, and `R.shuntT`. Preserve the existing numerical timing/multiplier unless another dependency genuinely requires adjustment. Boosted should continue to be active while this forward shove is active, just as the old HUD treated that short speed increase as part of Boosted.

Do not accidentally remove rear-end collisions, lane barging, shunts, ordinary boost, boost cans, ultimates, items, traps, seekers, wrecking, respawning, or finish-line run-out.

# Remove the airborne launch mechanic completely

Delete the full brake/wind-up/airborne subsystem, rather than merely disabling its input.

This includes the player-side and rival-side launch lifecycle: brake engagement, launch arming threshold, wind-up, launch power calculation, launch cooldown, airborne timer, air height/power/max duration, jumping clear of grounded cars or hazards, airborne contact immunity, airborne movement/speed multipliers, landing attacks, landing collision logic, launch particles, launch shadows, launch squat animation, and any AI strategy for launching.

Remove obsolete launch constants such as the `AIR_*` family used by this mechanic. Remove launch-only state including player and rival fields equivalent to `brakeOn`, `brakeKey`, `brakePtr`, `brakeSpent`, `brakeHeld`, `airMeter`, `airWind`, `airT`, `airMax`, `airPow`, `launchCD`, `airAim`, `airHold`, and `airWhy`.

Remove functions whose purpose is the airborne system, including the equivalents of `airPower`, `airborne`, `overhead`, `canBrake`, `airArmed`, `brakeHeld`, `brakeEngage`, `brakeOff`, `launchCar`, `landCar`, `killLaunch`, `updateLaunch`, `releaseBrake`, and all rival versions such as `rivalAirPower`, `rivalAirborne`, `canBrakeRival`, `rivalBrakeOff`, `launchRival`, `landRival`, `killLaunchRival`, and `updateRivalLaunch`.

After this work there should not be dormant air/launch state sitting at zero merely because the UI no longer reaches it. The mechanic itself must be gone.

Where functions such as `noContact`, `safeCar`, collision detection, hazard handling, or targeting currently contain an airborne exception, remove only the airborne part. Preserve the protection arising from respawn invulnerability, destruction/dead state, and being finished.

Remove the launch meter from both the normal DOM HUD and the split-screen canvas HUD. The bottom meter area should contain only the ordinary boost meter after this change. Reflow it deliberately rather than leaving a blank track where the launch meter used to be.

Remove the car lift, enlarged airborne rendering, launch shadow, wind-up squat, airborne flames, and any functions such as `airHop`, `rivalHop`, or launch-specific shadow rendering that become dead.

# Remove launch controls

The down-arrow / `S` brake-and-launch control must disappear. Do not silently remap it to another action.

Remove the corresponding keyboard down/up launch handling, touch swipe-down brake/launch handling, pointer brake state, controller down-stick/down-D-pad brake/launch handling, `humanBrake`, `padBrake`, and the launch-related `brake` snapshot in `newPadKeys` if it has no remaining purpose.

Update all help text and control legends so launch is no longer advertised. The home-screen control guide must not contain a Launch action.

Pointer/touch lane steering, swipe-up Boost, items, ultimates, pause, keyboard lane movement, ordinary Boost, and controller mappings unrelated to launch must continue to work.

Controller-disconnect cleanup should no longer try to release a nonexistent brake.

# Remove launch intelligence from the AI

The AI must stop reasoning about an action that no longer exists.

Remove the launch-specific difficulty dimensions currently represented by things such as `air` and `wind` if they have no remaining use. Remove launch-specific temperament properties such as `flair` if repository-wide inspection confirms they were only used for airborne launch decisions.

Delete launch decision functions such as `botAirWant` and all launch-related branches in `rivalThink`.

Remove `air` from AI field/sense representations. Remove airborne-only lane planning. Remove launch/brake penalties such as references to `R.brakeOn` from unrelated scoring functions.

Do not replace launch strategy with arbitrary compensating buffs. The AI should simply continue driving with its remaining lane choice, boost, item, collision, defensive, targeting, and ultimate systems.

All difficulty settings must remain functional after these fields disappear.

# Rename gameplay Effects to Conditions

This rename must be semantic and structural for the gameplay status system, not a dangerous global replacement of the English word “effect.”

Do not rename particle effects, `addFx`, VFX, audio effects, CSS animation effects, screen effects, visual effects, or other uses where “effect” has its normal graphics/audio/programming meaning.

Rename the gameplay-status data model from `EFFECTS` to `CONDITIONS`.

Rename gameplay-status functions, variables, comments, IDs, and UI concepts accordingly. For example, `effLog`, `effEls`, `effHost`, `logEffect`, `dropEffect`, `clearEffects`, `syncEffects`, `hudEffects`, `#effList`, `--eff`, and effect-specific CSS class names should become condition-oriented names or, where appropriate, be replaced entirely by the new derived icon system.

Prefer a single canonical condition definition table instead of scattering names, colors, category information, and icons around multiple renderers.

Each condition definition should contain at least its localization key, color, `buff`/`debuff` classification, and icon identifier. Include icon foreground/ink metadata if necessary for contrast.

The condition system should derive what is active from authoritative racer state. Do not create a second mutable condition truth that can drift out of sync with `slowT`, `blind`, `slipT`, Boost state, invulnerability timers, etc.

A helper conceptually equivalent to `activeConditions(who)` should be the canonical way both world badges and HUD badges ask, “Which Conditions does this racer have right now?”

The order returned by that helper must be deterministic so stacked icons do not randomly reorder between frames. A reasonable priority is Invulnerable, Boosted, Slowed, Obscured, Skidded.

Finished racers should return no active Conditions for presentation, because they are no longer participating in the race.

# Rename Immune to Invulnerable properly

Change the user-facing condition `Immune` to `Invulnerable`.

Also rename the underlying temporary gameplay terminology where practical so the code and UI agree. This should include the temporary racer timer/state currently called `immune`, `IMMUNE_TIME`, helpers like `immuneCar`, `immuneMe`, `immuneWho`, `immuneScrub`, dedicated DOM/CSS names such as `#immuneTag` or `.immune`, relevant comments, and localization keys.

Prefer names such as `invulnerable`, `INVULNERABLE_TIME`, and suitable `invulnerable...` helpers.

Do not conflate temporary Invulnerable with finish protection.

Architecturally, keep these concepts separate:

Temporary Invulnerable means an active positive Condition, normally coming from respawn protection or whatever existing gameplay grants that timer.

Finished means the racer has crossed the finish line and is permanently out of play for this race.

A shared internal helper may decide whether something is touchable/targetable, but `activeConditions()` must only return `invulnerable` when the actual temporary invulnerability state is active. It must not display Invulnerable simply because the racer has finished.

Rename the cleanup terminology from vague “bad effects” language to Conditions language, such as clearing debuffs.

Invulnerable must continue refusing/clearing negative Conditions exactly as the existing protection intends.

# Rename Cluttered to Obscured

Replace the gameplay condition identifier `cluttered` with `obscured`.

Update its localization key, info text, Conditions-tab entry, HUD/world badge lookup, comments, and every gameplay-status reference.

The underlying water/puddle visual behavior may continue to use an internal field such as `blind` if that field describes a rendering timer and changing it would create unnecessary risk, but no player-facing condition or gameplay status API should still call the condition Cluttered.

Update puddle descriptions so they say the racer becomes Obscured rather than Cluttered.

# Rename Slippery to Skidded

Replace the gameplay condition identifier `slippery` with `skidded`.

Update its localization key, description, Conditions tab, icon, state-to-condition mapping, comments, and all player-facing uses.

The underlying steering timer may remain `slipT`/`slip` if renaming that physical mechanic introduces unnecessary unrelated churn, but the Condition presented by that state must be Skidded everywhere.

The existing meaning remains unchanged: steering is reversed while this debuff is active.

# Completely remove Winner as a Condition while preserving finished-racer protection

Delete `winner` from the condition definition table.

Delete `efWinner`, `winnerInfo`, Winner condition rendering, Winner condition logging, Winner condition cards, and any other presentation of Winner as a status/condition.

Do not replace it with a condition called Finished.

Do not add a finish icon.

Keep `G.finished`, `R.finished`, finishing place assignment, `G.results`, parking/run-out, final placement, finish order, and results behavior.

Once a racer crosses the line, that racer must immediately become out of play and remain out of play. Nothing may select it as an offensive target. Nothing may track it. Nothing may collide with it. Nothing may apply damage, wreck it, slow it, obscure it, skid it, hit it with an item, hit it with a hazard, hit it with a seeker, or otherwise alter its finished result. Existing offensive target selection and collision systems must consistently exclude finished racers.

Audit seeker targeting in particular. A finished racer must never be selected. If a seeker or other tracking object was already locked to a racer at the exact moment that racer finishes, it must lose that target harmlessly, fade/despawn, or otherwise be guaranteed incapable of interacting with that racer.

Audit AI targeting and threat evaluation as well. Finished racers should be out of the actionable field, not merely immune when the eventual hit occurs.

Finished cars should not themselves continue firing items, using ultimates, barging, or otherwise participating after crossing. Preserve the current run-out/parking animation only.

Remove comments that call this protection “Winner.” Use terms such as “finished racer,” “finisher,” “out of play,” or “finish protection.”

# Replace in-race condition text with colored icon circles

Use Ponu as the visual inspiration: compact colored circular markers with a strong, simple icon/device inside them. The icon should be recognizable from shape as well as color.

For Seren, these are condition badges, not copies of Ponu’s marble bodies.

Each badge should be a small filled circle using that Condition’s canonical color, with a high-contrast icon centered inside. Use the same icon artwork everywhere the Condition appears. Do not use emoji, text letters, or externally loaded image files. Draw icons using Canvas paths for canvas rendering and equivalent inline SVG/CSS geometry for DOM rendering, or establish one compact reusable icon representation that both presentation paths can consume reliably.

Bright backgrounds such as Invulnerable gold and Boosted orange may use dark icon ink. Dark backgrounds such as Skidded must use a white/light icon and a visible light outline so the badge does not vanish against Seren’s dark HUD or road.

The badge must remain legible at Seren’s smallest supported scene scale and in split-screen.

# Rival-car condition badges

Every visible car that is not the owner of the current camera/view must show its active Conditions as small circular icon badges adjacent to the car.

This applies equally to AI racers and human-controlled rival racers.

Do not special-case AI versus human. The source of input is irrelevant; the racer’s state determines its Conditions.

In normal single-player play, the local/client car is the view owner and therefore receives no condition circles beside the physical car. Its Conditions appear in the bottom-left HUD instead.

In local split-screen, evaluate this independently for every viewport using the existing view owner concept such as `VOWN`. In Player 2’s viewport, Player 2 is the owner and therefore gets no beside-car condition badges there; Player 1, Player 3, Player 4, and bots may receive them if visible. In another player’s viewport, Player 2 is a rival and therefore does receive world-space badges.

This distinction is essential.

Anchor the rival badge stack just outside the car silhouette, preferably beside the upper/right side. If that would cross the current viewport boundary, flip it to the other side or clamp it so it remains visible. It must not be positioned in global full-canvas coordinates that cause one split-screen column’s badges to leak into another.

When the racer has several Conditions, stack the circular badges vertically above one another in a compact column. The stack must be stable and deterministic. Do not overlap the car’s player-number/seat marker unnecessarily; adjust the anchor sufficiently to keep both readable.

Only show these beside-car badges for racers actually visible in the current viewport. Do not invent new condition badges on the existing off-screen racer arrows/markers unless necessary to prevent a regression.

Badges should follow the car’s rendered location during ordinary lane interpolation and camera movement.

Finished racers get no badges.

# View-owner/player condition HUD

The view owner must not have condition icons floating beside their own car.

Instead, replace the current bottom-left text-based effect notifications with the same circular Condition badges.

Remove the old text pills entirely.

The bottom-left condition area should use the same canonical colors and icons as rival badges. Do not display the condition names during active racing.

If several Conditions are active simultaneously, stack their icons vertically upward from the current bottom-left effect-notification position. Keep the order deterministic.

The Invulnerable condition must also use this bottom-left badge system. Remove the separate text `immuneTag`.

If Seren’s existing gold screen-edge feedback for temporary immunity is retained as secondary feedback, rename it to Invulnerable terminology and keep the condition icon as the canonical HUD indicator. There must be no Immune text left.

In local split-screen, each canvas HUD must render the Conditions of that viewport’s owner in its own bottom-left corner. The page-level DOM condition host should remain hidden in local mode just as the old page HUD was, because each split-screen view paints its own instruments.

# One canonical icon renderer

Avoid implementing five icons separately in three different places.

Introduce an authoritative icon definition/renderer layer. A function such as `drawConditionIcon(ctx, id, cx, cy, radius)` can render canvas badges for rivals and split-screen HUDs.

For the DOM garage/HUD, use the same icon identifiers and equivalent vector shapes. If a clean helper can generate inline SVG safely, use that. Do not duplicate condition classification, names, or colors in HTML-generation functions.

The icon artwork should remain intentionally simple: Shield for Invulnerable; forward chevrons/lightning for Boosted; reduced-speed/downward mark for Slowed; obscured/crossed-eye or water-blocked visibility symbol for Obscured; curved paired skid marks for Skidded.

# Rename the Garage Effects tab to Conditions

Rename the main `Effects` tab to `Conditions`.

Rename the DOM ID from `tabEffects` to `tabConditions`, the garage internal key from `"effects"` to `"conditions"`, tab keyboard-navigation arrays, event wiring in `main.js`, ARIA links, localization keys, and any associated comments.

The main garage tab order should remain Cars, Tracks/traps, Mystery bubbles, Conditions unless there is a strong existing layout reason not to.

Inside Conditions, add a secondary two-tab switcher:

`Buff`

`Debuff`

The Conditions page should default to Buff whenever appropriate, while preserving a user’s current Buff/Debuff selection during simple rebuilds if that fits the existing garage architecture.

Buff contains exactly Boosted and Invulnerable.

Debuff contains exactly Slowed, Obscured, and Skidded.

Launched must not appear.

Winner/Finished must not appear.

Each Conditions reference card must show the same colored circular icon directly next to the condition name, followed by the existing style of explanatory text.

The Buff/Debuff split should be generated from `CONDITIONS[id].type` rather than maintaining a separate hard-coded list that can drift from the data model.

Give the secondary tabs proper button/tab semantics, visible selected state, keyboard focus, and sensible arrow-key behavior consistent with the existing main garage tabs.

# Localization

Update both English and French; do not leave one language with stale names.

Replace `tabEffects` with a Conditions localization key.

Add localization for Buff and Debuff.

Replace the old `ef*` gameplay-status naming scheme with condition-oriented keys if practical, for example `condBoosted`, `condInvulnerable`, `condSlowed`, `condObscured`, and `condSkidded`.

Remove launched strings and launched help text.

Remove Winner condition strings and Winner condition description.

Rename Immune strings/descriptions to Invulnerable.

Rename Cluttered strings/descriptions to Obscured.

Rename Slippery strings/descriptions to Skidded.

Update any sentences in item, puddle, trap, settings, controls, help, or garage descriptions that use old terminology.

Change the custom rule label currently equivalent to `Boost and launch` to simply `Boost`, and rewrite its description so it describes only the Boost meter/mechanic.

Remove the Launch control label.

Preserve correct French grammar and terminology rather than simply leaving English words in the French table.

# HUD and custom-race cleanup

The existing custom rule key `boost` may remain the switch governing ordinary boost. It must no longer imply or reference airborne launch.

The standard boost meter must continue functioning exactly as before.

Ordinary Boost, boost cans, ultimate speed, and the rear-contact shunt should all still map to the Boosted Condition when they make the racer faster, matching Seren’s existing “anything that makes you faster” concept.

The removed airborne mechanic must not leave a second empty meter, a blank HUD slot, hidden keyboard action, unused controller state, dead custom-setting copy, or stale localized text.

# Source files to inspect carefully

Do not limit edits to these files, but explicitly inspect `js/data.js`, `js/runtime.js`, `js/mechanics.js`, `js/race.js`, `js/ai.js`, `js/input.js`, `js/local.js`, `js/render.js`, `js/hud.js`, `js/ui.js`, `js/i18n.js`, `js/main.js`, `index.html`, and `css/app.css`.

Also inspect `README.md`, `docs/ARCHITECTURE.md`, `docs/TUNING.md`, other QA documentation, `upd` if it is maintained as release/update documentation, and the scripts under `tools/`.

Search the entire Seren repository before declaring the work complete.

Be especially careful because Seren uses classic shared-scope scripts. Do not introduce duplicate top-level declarations or change script dependency order casually.

# Architecture requirement

The final implementation should have one authoritative `CONDITIONS` table and one authoritative active-condition derivation path.

World rendering, normal HUD rendering, split-screen HUD rendering, and the Conditions reference page should all read from the same condition definitions rather than duplicating colors or categories.

Do not make the DOM notification system maintain timers merely to decide whether a Condition exists. The gameplay timer/state should remain authoritative. UI should reflect that state.

Where transitions/fades are desirable, animation state may exist for presentation only, but it must never keep a Condition visible after the gameplay state has ended or hide a Condition that is active.

# Accessibility and visual quality

Conditions must never rely on color alone. The icon shape is mandatory.

The Garage Conditions page supplies the text/name/description key for learning what the symbols mean.

DOM condition badges should have useful accessible labels even though their visible in-race form is icon-only.

Honor the existing reduced-motion setting. Condition badges do not need constant pulsing/animation. If appearing/disappearing is animated, reduced-motion should make it effectively immediate/static.

Do not introduce dependencies, external icon libraries, image downloads, web fonts, or build tooling.

Match Seren’s existing visual style rather than importing Ponu’s entire aesthetic. Ponu is the reference for the compact colored-circle-with-distinct-icon concept only.

# Regression requirements

Normal steering must still work.

Boost must still work.

Boost depletion/recharge must still work.

Ultimates must still work.

Items must still work.

Oil must still reverse steering and now display Skidded.

Puddles must still impair visibility and now display Obscured.

Slowdowns must still display Slowed.

Respawn protection must still function and display Invulnerable while the actual temporary timer is active.

Rear-end shunts must still provide their existing short forward speed effect under their newly renamed shunt identifiers and display Boosted.

Finished racers must remain permanently excluded from interactions with no visible Condition badge.

AI and human rivals must use identical Condition detection.

Single-player and every supported local split-screen player count must render condition icons correctly.

# Repository cleanup and verification

Perform a case-sensitive and case-insensitive repository search after implementation.

There should be no remaining gameplay condition named `launched`, no launch mechanic functions/fields/constants, no launch meter, no launch control instruction, and no launch AI.

There should be no gameplay condition named `winner`.

There should be no gameplay-status identifier or user-facing label named `immune`, `cluttered`, or `slippery` where it refers to these renamed Conditions.

There should be no main Garage tab named Effects.

There should be no old `EFFECTS` gameplay-status table.

Do not treat legitimate phrases such as particle effects, visual effects, sound effects, or a missile being “launched” in ordinary English as errors. The goal is removing the airborne launch mechanic and renaming the gameplay status/effect concept, not blindly banning English words from unrelated systems.

After editing, run Seren’s existing validation tooling. At minimum run `node tools/check.mjs`. Also run the repository’s menu and gameplay/ultimate QA scripts using their normal invocation and fix regressions rather than weakening the tests.

Update/add regression coverage so the checks catch the new Conditions tab and its two sub-tabs, all five condition definitions, Buff/Debuff classification, the absence of Launched/Winner conditions, removal of obsolete launch DOM IDs, correct localization keys, finished-racer target exclusion, and the new condition icon rendering/state helpers where feasible.

Do not delete a failing assertion merely because it describes old launch behavior; replace obsolete launch-specific tests with assertions proving the launch system is actually absent and the remaining mechanics still function.

# Completion standard

Do not consider this task complete merely because the UI looks right.

It is complete only when all of the following are true:

1. The airborne brake/wind-up/jump/landing mechanic is genuinely removed from player gameplay, rival gameplay, AI, runtime state, input, rendering, HUD, configuration, documentation, localization, and tests.
2. The unrelated rear-impact forward shove still works and has been renamed away from launch terminology so it cannot be confused with the deleted mechanic.
3. `EFFECTS` has become an authoritative `CONDITIONS` model containing exactly Boosted, Invulnerable, Slowed, Obscured, and Skidded.
4. Boosted and Invulnerable are Buffs; Slowed, Obscured, and Skidded are Debuffs.
5. Launched no longer exists as a Condition.
6. Winner no longer exists as a Condition.
7. Crossing the finish line still immediately and permanently removes a racer from all targeting, tracking, collision, hazard, attack, debuff, and damage interactions while preserving placement/run-out/results.
8. Finish protection is never shown as a Condition and is not called Winner.
9. Temporary Immune has become Invulnerable throughout the appropriate gameplay/status code and UI without accidentally conflating it with finished-racer protection.
10. Cluttered is now Obscured and Slippery is now Skidded throughout the gameplay-status presentation.
11. The Garage tab is Conditions, with accessible Buff and Debuff secondary tabs and the correct entries under each.
12. Every Condition has exactly one canonical color and one canonical recognizable icon.
13. Every non-view-owner visible racer, AI or human, receives adjacent world-space condition circles, with multiple conditions stacked vertically.
14. The current view-owner racer never receives those circles beside its own car; its conditions instead appear as icon-only circles in the bottom-left HUD where the old effect text lived.
15. Local split-screen evaluates “view owner” separately per viewport, so another human is treated as a rival in views they do not own.
16. No old text effect pills or separate Immune text tag remain.
17. No blank launch meter, dead launch control, dormant launch state, or launch-specific AI tuning remains.
18. All existing non-launch gameplay continues functioning and all repository checks pass.
19. English and French remain complete and consistent.
20. The implementation contains no duplicated condition metadata or brittle special cases that will immediately drift apart.

Work carefully, inspect the entire dependency chain before deleting anything, and favor a coherent small refactor over patches that merely hide the old systems.
