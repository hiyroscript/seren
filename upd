You are modifying the GitHub repository hiyroscript/seren.

Work against the latest main branch of Seren. Do not make superficial text-only changes. This is a focused rendering/data cleanup that changes one racer’s identity and visual model, deletes an entire dormant civilian-traffic subsystem, and correctly integrates Flann’s tailpipe boost effect while preserving the rest of Seren’s gameplay.

The new vehicle artwork already exists in the repository root as:

v_flann.PNG

Use that exact filename and case. GitHub Pages and other hosts may be case-sensitive.

Primary objectives

Replace the existing racer Redd completely with Flann.

Flann must use v_flann.PNG as the actual in-game vehicle sprite instead of Redd’s current procedurally drawn Canvas GT model.

Rename Redd to Flann everywhere, including the internal car ID. This is not merely a display-name change. The final current source tree must use flann, not redd, as the canonical identifier for this car.

Completely delete Seren’s civilian/NPC traffic system and all of its dormant code. Do not confuse civilian traffic with the six racing cars. Rival bots and local-player racers are core gameplay and must remain.

Finally, replace the generic boost-flame positioning on Flann with an exhaust effect that is anchored to the actual tailpipes in v_flann.PNG, rotates with the sprite, scales with it, and animates smoothly without jitter.

Repository architecture you must respect

Seren is deliberately build-free. It consists of index.html, css/app.css, and fifteen classic deferred JavaScript files sharing one global script scope.

The current load order is:

core → i18n → data → audio → runtime → ui → settings → local → ai → mechanics → race → render → hud → input → main

Do not introduce a bundler, framework, package dependency, module system, or build step.

Every top-level JavaScript name must remain unique across all scripts. Do not introduce duplicate const, let, var, or function names.

Keep "use strict"; at the top of every existing JavaScript source file.

Preserve the current Canvas 2D architecture.

Do not rewrite unrelated systems.

First: inspect before editing

Before changing anything, perform a repository-wide inspection for every occurrence and dependency involving:

redd
Redd
carRedd
reddUlt
CARS
CAR_IDS
TEMPERS
G.car
drawCar
drawGT
paintCarIcon
paintCarIcons
previewCar
traffic
TRAFFIC_ENABLED
TRAFFIC_PAINT
G.traffic
relGap
nextGap
spawnWave
breakWalls
crash
overLead
laneRisk
flames
boosting
ultOn

Also search all documentation and tests, not only runtime JavaScript.

Do not assume the references described in this prompt are exhaustive. Follow dependencies in the current source.

Flann must fully replace Redd as an identity

In js/data.js, replace the existing CARS.redd definition with a canonical CARS.flann definition.

Its identity must use:

key: "flann"

and the sprite source must resolve to:

v_flann.PNG

Use a clean sprite-oriented model rather than pretending the image is another procedural "gt" body.

For example, the data model may use a dedicated style: "sprite" and/or a sprite: "v_flann.PNG" property. Choose whichever integrates most cleanly with the existing renderer, but the end result must make it unambiguous that Flann uses image artwork.

Retain any Flann properties still needed by shared gameplay, such as accent and flame, because shared mechanics use car colors for particles, ultimate bursts, collisions, etc.

Do not retain obsolete body, glass, dark, or other procedural-GT properties merely because Redd previously needed them unless another live consumer genuinely still reads those values.

Change:

CAR_IDS = ["redd", ...]

to use:

"flann"

in the same roster position.

Flann must remain one of the six normal selectable/raceable cars and must behave mechanically exactly as Redd behaved unless this prompt explicitly says otherwise.

Preserve Redd’s existing temperament values, but move them to:

TEMPERS.flann

Update the fallback in makeTemper() from TEMPERS.redd to TEMPERS.flann.

Update comments such as the existing statement that Redd is the brawler so that they refer to Flann.

In js/runtime.js, change the default selected car from:

car: "redd"

to:

car: "flann"

Search for every other hard-coded "redd" reference in gameplay code and replace it appropriately.

There must be no compatibility alias such as redd: flann, no duplicate Redd entry, and no hidden old ID left behind. The current game should have Flann, not “Redd renamed only on screen.”

Rename all UI and localization surfaces

In js/i18n.js, replace the localization keys:

redd
reddUlt

with:

flann
flannUlt

The English and French visible car name should both be:

Flann

Preserve the existing ultimate description unless another current dependency requires a wording correction. Flann’s ultimate is mechanically unchanged.

Remove the old redd and reddUlt translation keys completely once nothing references them.

In index.html, replace the first car’s DOM identity.

The current elements using concepts such as:

id="carRedd"
data-car="redd"
data-i18n="redd"
visible text Redd

must become their Flann equivalents:

id="carFlann"
data-car="flann"
data-i18n="flann"
visible fallback text Flann

The showroom hero’s default data-car must also become flann.

In js/main.js, replace the current direct click wiring for #carRedd / pickCar("redd") with #carFlann / pickCar("flann").

Because ui.js derives selectors through:

carEl(id) => "#car" + cap(id)

the new flann ID and carFlann DOM ID must agree exactly.

Update any hard-coded Redd references in tests.

Examples that currently need attention include tools/menu-check.mjs and tools/ultimate-check.mjs, where tests select carRedd or explicitly call setup with "redd".

Do not weaken or remove tests simply to make the rename pass. Update them to exercise Flann instead.

Replace Redd’s procedural GT renderer with v_flann.PNG

The current drawCar() dispatches car definitions into hand-written procedural Canvas models.

Flann must no longer be drawn by drawGT().

Create a dedicated Flann image-sprite rendering path.

The sprite must be loaded once and reused. Never construct a new Image() every frame.

The asset must be ready for all places where the common drawCar() path is used:

the live race,
rival rendering,
local split-screen,
the car selection screen,
the showroom hero,
and the Cars & more / garage reference cards.

The existing UI intentionally uses the same drawCar() path for menu artwork and in-race artwork. Preserve that architectural property. Do not create a separate HTML <img> representation that visually diverges from the actual race sprite.

A clean implementation would give drawCar() a Flann/sprite branch and leave the other five procedural cars on their existing renderers.

Load the sprite early enough that menus do not permanently paint a blank canvas before the asset arrives.

If the initial menu paint occurs before image decode completes, the image load callback must trigger a repaint of car canvases. The continuously rendered race will naturally pick it up on subsequent frames.

Do not perform asynchronous image work per frame.

Use the image’s real naturalWidth and naturalHeight to preserve its aspect ratio. Do not squash or stretch v_flann.PNG into the old procedural Redd proportions.

The visible Flann sprite must remain centered on the same logical car position used by all mechanics:

x, y

The image must rotate around the car center when tilt is applied.

The vehicle’s nose must point in the same direction as every other Seren car: toward negative local Y / the top of the screen.

Do not rotate the source incorrectly or mirror it.

The entire vehicle should remain visible. Do not clip its nose, rear, spoiler, tires, or other sprite details.

Keep collision/gameplay geometry independent from artwork. carW, carH, lane collision rules, rear-ending, barging, hazards, finish behavior, AI, etc. should not change merely because the visible drawing became an image.

Tune a visual sprite scale if necessary so Flann occupies approximately the same gameplay footprint as the other racers without changing the logical collision dimensions.

If the PNG contains transparent padding, account for it deliberately when choosing the display scale rather than distorting the sprite or changing collision mechanics.

Remove the old procedural GT model when it becomes dead

The existing drawGT() is currently described as the model for “Redd and all traffic.”

After this task:

Redd no longer exists,
Flann uses v_flann.PNG,
civilian traffic no longer exists.

Therefore, verify whether anything still calls drawGT().

If nothing does, delete drawGT() completely.

Do not leave a large unused Redd/traffic procedural model hidden in render.js.

Update drawCar() so its dispatch has no fallback that accidentally turns an unknown car into the obsolete GT/Redd model. Unknown models should not silently become Redd.

Preserve the procedural renderers for Phantom, Bolt, Timestamp, Rose, and Siren.

Flann shadow and sprite layering

Flann should still visually belong on the road.

Render a subtle vehicle shadow beneath the sprite using the same general visual weight as the other cars.

The shadow must not replace or obscure the image.

The recommended local layering inside Flann’s translated/rotated coordinate system is:

shadow,
boost exhaust,
Flann sprite,
then any externally rendered race overlays such as player seat markers or Condition badges.

The exhaust should appear to originate underneath/behind the rear of the vehicle rather than being painted over the bodywork.

All Canvas state modifications must be safely contained with ctx.save() / ctx.restore() or equivalent restoration.

Do not leak globalAlpha, globalCompositeOperation, shadows, transforms, filters, or gradients into the next renderer.

Correct Flann’s tailpipe boost effect

The current generic flames(w, h, ...) routine uses fixed rectangular exhaust locations based on the abstract car box.

That positioning is not sufficient for the PNG.

Flann needs sprite-specific tailpipe/exhaust anchors based on the actual artwork in v_flann.PNG.

Inspect the sprite itself and locate the visible rear exhaust/tailpipe positions.

Do not guess from Redd’s old geometry.

Store the two exhaust anchors in one authoritative location, preferably as normalized coordinates associated with Flann’s rendering data or in clearly named Flann-render constants.

For example, an anchor representation can conceptually be expressed relative to the sprite’s local visual rectangle rather than hard-coded absolute screen pixels.

Both exhaust points must scale automatically when the sprite scales.

They must move automatically when Flann changes lane.

They must rotate automatically when Flann tilts.

They must work identically for player Flann and rival/bot Flann.

They must also stay correct in every local split-screen column.

The exhaust direction is toward the rear of the vehicle, which in Flann’s unrotated local space means positive Y / downward on screen.

The flame roots should disappear slightly underneath the tailpipe/rear body so there is no visible gap between car and plume.

The exhaust must not originate from the centre of the bumper unless the actual image tailpipes are located there.

Do not draw the flame over Flann’s cabin, rear glass, spoiler, lights, or body.

Smooth exhaust animation

Do not simply reuse the current four static rounded rectangles for Flann.

Create a dedicated Flann exhaust drawing routine.

It should produce two small, clean arcade-racing plumes with a tapered shape rather than two rigid blocks.

Use the existing car flame palette as the basis so shared color data remains meaningful.

The effect may use a layered hot/cool core, alpha falloff, Canvas gradients, or similarly lightweight Canvas 2D techniques.

Animation must be smooth and deterministic enough that it does not buzz or jump.

Do not call Math.random() independently on every rendered frame to reposition the entire flame, because that produces visual jitter.

Use a smooth periodic pulse, interpolation, sine-based variation, or another continuous visual phase.

The effect can vary flame length and width modestly over time while the root remains pinned to the exhaust.

If motionReduced() is active, suppress or substantially reduce the pulsing/flicker while keeping a readable static boost plume.

The visual animation must not mutate gameplay state.

Boost activation semantics

Preserve Seren’s existing gameplay mechanics.

The current race renderer supplies the visual boost flag using the existing boost/ultimate state for player and rivals.

Do not alter charge drain, refill rates, speed multipliers, ultimate timing, AI decisions, or Conditions merely to implement the visual.

Flann’s exhaust should turn on and off from the same authoritative visual boost state that the other vehicle renderers currently receive.

When no boost effect is active, there must be no residual exhaust plume.

When the effect activates or deactivates, it should remain visually stable and correctly anchored.

Do not add exhaust flames to menu/garage previews, because those canvases call the shared renderer with boost inactive.

Completely remove civilian NPC traffic

This means deleting the subsystem, not disabling it.

Civilian traffic is separate from Seren’s racer field.

Do NOT remove:

G.rivals,
bot racers,
bot AI,
local multiplayer opponents,
the six-car field,
racer-to-racer barging,
rear-end collisions between racers,
racer targeting,
standings,
or finish logic.

Remove only the non-racer civilian road traffic system.

Currently this subsystem is dormant because TRAFFIC_ENABLED is false, but its implementation is still distributed through the project.

Delete TRAFFIC_ENABLED from js/core.js.

Delete its comments explaining that “other cars” can be switched back on.

Delete TRAFFIC_PAINT and all civilian-paint definitions from js/data.js.

Delete civilian-traffic runtime state from G in js/runtime.js, including traffic and any counters that exist only to spawn civilian traffic, such as relGap and nextGap, after verifying they have no unrelated use.

Delete corresponding startRace() resets such as:

G.traffic = []
G.relGap = ...
G.nextGap = ...

Delete spawnWave() from js/mechanics.js.

Delete breakWalls() from js/mechanics.js.

Before deleting neighboring helpers such as onScreen(), search whether they are used by traps, particles, missiles, wrecking, or any other surviving mechanic. Remove a helper only if it genuinely becomes dead.

Delete the complete civilian traffic update block from update() in js/race.js, including:

traffic movement,
lane interpolation,
offscreen removal,
the civilian overtake/passed logic,
the extra-distance/tone reward,
civilian collision tests,
calls to crash(),
traffic spawning,
traffic spawn-gap accumulation,
spawnWave(),
and breakWalls().

Delete the civilian traffic draw loop from js/render.js.

Delete the G.traffic loop from laneRisk() in js/ai.js.

Bot racers must still evaluate hazards and actual racers normally.

Rewrite the laneRisk() comments so they no longer claim bots are avoiding civilian traffic.

Remove traffic-only dead code

After deleting civilian traffic, trace crash().

The current civilian collision path appears to be the caller of the traffic-specific crash() race-over behavior.

If repository-wide inspection confirms crash() has no remaining legitimate caller, delete it completely.

Do not leave an unreachable special race-over function behind.

Similarly trace the localized overLead copy currently saying:

“You clipped traffic.”

If the removed crash flow was its only meaningful use, remove that localization entry and any reset/assignment such as:

$("#ovLead").textContent = t("overLead")

that exists solely to support this dead path.

Do not break normal race result copy. Finish/result flows for Race against bots and Local play must continue to set and display their existing result text.

The over lifecycle state itself may still be used by legitimate finishing/result behavior, so do not delete a broader lifecycle concept just because the traffic crash path disappears.

Remove obsolete traffic text from the UI

The current Endless description still says:

“Three lanes, dense traffic, no finish line. Go as far as you can.”

That is already inaccurate while TRAFFIC_ENABLED is false and must be corrected now that civilian traffic is actually gone.

Use copy equivalent to:

English:
“Three lanes, no finish line. Go as far as you can.”

Provide an accurate natural French equivalent as well.

Update both the STR translation and the static fallback text in index.html so there is no flash/stale fallback mentioning dense traffic before localization runs.

Search the rest of index.html and js/i18n.js for traffic-specific text and remove or rewrite it as appropriate.

Do not remove descriptions of racer bots merely because they are computer-controlled. They are racers, not civilian traffic.

Clean documentation thoroughly

Update README.md.

Replace Redd with Flann in the car table and any other prose.

Document that Flann uses the sprite asset v_flann.PNG if that level of rendering detail belongs in the existing project-layout/rendering documentation.

Remove or rewrite any gameplay statement implying civilian traffic is present.

Do not inaccurately describe Endless as containing civilian traffic after this change.

Update docs/ARCHITECTURE.md.

Remove traffic from the documented G world fields.

Remove the documentation of TRAFFIC_ENABLED from core.js.

Update rendering documentation if appropriate to explain that Flann is the image-backed car while the remaining cars use Canvas models.

Keep the important invariant that showroom/reference artwork still comes through the same drawCar() path as race artwork.

Update docs/TUNING.md.

Delete the TRAFFIC_ENABLED entry.

Delete the TRAFFIC_PAINT entry.

Delete the entire obsolete Traffic section describing how flipping the flag brings civilian cars back.

Remove traffic spawn/tuning discussion.

Update any Redd references to Flann.

Search all remaining Markdown files under docs/ for Redd and civilian-traffic assumptions, including QA documentation. Update anything that describes current behavior.

Do not edit historical Git commit history; only current tracked files matter.

Tests and fixtures

Update all automated tests and fixtures affected by the internal rename.

At minimum, inspect:

tools/check.mjs
tools/game-fixture.mjs
tools/menu-check.mjs
tools/ultimate-check.mjs

tools/menu-check.mjs currently contains explicit interactions with carRedd. Those should test carFlann.

tools/ultimate-check.mjs iterates CAR_IDS, which should naturally pick up Flann, but it also contains explicit "redd" setup cases. Change those explicit cases to "flann" so Conditions, finish protection, rendering, and other regression coverage remain intact.

Do not special-case Flann out of any all-cars test.

The rendering smoke tests must continue to invoke render() successfully for all six cars.

Because the test Canvas is a lightweight proxy, make image loading code test-safe. Do not make the Node fixture require a real browser image decoder merely to load the source files.

The fixture already supplies an Image test double. Design the loader so that loading the browser image does not throw in this environment.

Important cleanup invariant

After the implementation, run repository-wide searches.

There should be no current source reference to Redd or the old Redd ID.

A search equivalent to:

rg -ni '\bredd\b' .

should return no legitimate current-game references.

Likewise, these civilian-traffic implementation identifiers should be completely absent after cleanup:

TRAFFIC_ENABLED
TRAFFIC_PAINT
G.traffic
spawnWave
breakWalls

Search for relGap and nextGap; if their only use was civilian traffic, they must also be gone.

Search for "traffic" case-insensitively and manually inspect every result. Remove stale civilian-traffic descriptions/comments while preserving only genuinely unrelated uses, if any.

Search for drawGT. It should be gone if nothing survives that needs it.

Search for crash() and overLead; they should be gone if the removed civilian collision path was their only purpose.

Do not leave commented-out code for any of these systems.

Preserve unrelated gameplay exactly

Do not change the six-racer field.

Do not reduce the number of racer bots.

Do not alter bot difficulty or temperament beyond changing the temperament map key from redd to flann.

Do not alter Flann’s performance relative to Redd.

Do not alter base speed.

Do not alter boost drain/refill.

Do not alter the ×1.5 ordinary boost multiplier.

Do not alter the ×2 ultimate multiplier or its 15-second duration.

Do not alter ultimate charge rate.

Do not alter Boosted/Invulnerable/Slowed/Obscured/Skidded Conditions.

Do not change hazards.

Do not change mystery bubbles or items.

Do not change oil, seekers, boost cans, traps, wrecking, respawns, finish protection, lane barging, racer rear-ending, standings, local multiplayer, split-screen behavior, input, audio, settings, saved best distance, or race structure except where a stale civilian-traffic description must be corrected.

Do not rename Flann’s sprite file unless absolutely necessary. Prefer using the existing exact v_flann.PNG.

Rendering acceptance criteria

Flann appears in the first roster slot where Redd previously appeared.

Every user-visible name says Flann.

There is no Redd entry.

Selecting Flann starts the race with Flann.

Bot Flann also uses the exact PNG sprite.

The same sprite appears in the car selection card, showroom hero, garage/reference page, normal race, and split-screen race.

The image is not stretched.

The entire vehicle is visible.

The vehicle is centered correctly in its lane.

The front points up-road.

Lane-change tilt rotates the sprite naturally around its center.

Invulnerability blinking still works because the outer race renderer controls visibility, not the model.

Condition stacks and player markers still render correctly relative to Flann.

The boost exhaust originates from the two actual tailpipes.

The plume roots stay attached while the car tilts.

The exhaust stays attached at different canvas sizes and DPRs.

The exhaust stays attached in 2-, 3-, and 4-player split screen.

Flames never float below the car with a visible gap.

Flames never originate from an obviously wrong part of the bumper.

Flames do not draw across the sprite body.

Animation is smooth rather than random/jittery.

Reduced-motion mode produces a stable lower-motion effect.

No exhaust is visible while boost is inactive.

No civilian traffic vehicle is ever spawned, updated, collision-tested, rendered, or considered by AI because the subsystem no longer exists.

Behavioral acceptance criteria

Endless continues running until the player leaves or another legitimate existing lifecycle rule ends it; there is no hidden civilian-traffic crash path.

Race against bots still contains the player plus the normal racing field.

Local play still contains humans plus racer bots filling the remaining seats.

Bot lane selection still avoids hazards and other actual racers.

All racer-vs-racer collision mechanics remain.

Flann’s temperament remains equivalent to old Redd’s temperament.

Random car selection can select Flann.

Taken-car handling in local multiplayer works for Flann.

The garage displays six cars.

All localization keys resolve.

No selector references a removed #carRedd.

Validation

Run the existing automated checks after implementation:

node tools/check.mjs

node tools/menu-check.mjs

node tools/ultimate-check.mjs

Fix actual failures rather than weakening assertions.

Then manually exercise at least:

single-player Endless with Flann,
Race against bots with Flann as player,
Flann as a rival bot while driving another car,
ordinary boost on Flann,
Flann ultimate,
Flann changing lanes while boosting,
Flann invulnerability blink,
Flann Condition rendering,
car selection/showroom artwork,
garage artwork,
2-player split screen with Flann,
and a long enough run to confirm civilian traffic never appears and no traffic-specific crash behavior remains.

Also test at narrow portrait size and desktop size.

Implementation quality

Prefer a small coherent sprite abstraction over scattering if (car === "flann") checks throughout unrelated code.

Keep the sprite source, render dimensions/scaling, and tailpipe anchors authoritative rather than duplicated.

Keep rendering code render-only.

Keep gameplay collision geometry separate from sprite pixel geometry.

Do not mutate race state from the Flann visual animation.

Do not use per-frame DOM elements for flames.

Do not create per-frame Image objects.

Do not introduce external assets, libraries, CSS frameworks, dependencies, or network requests.

Do not leave dead Redd code.

Do not leave dead civilian traffic code.

Do not leave compatibility shims for systems intentionally being removed.

Final deliverable

Implement the change completely rather than stopping after the first visible success.

When finished, provide a concise implementation report containing the files changed, how Flann’s sprite loading/rendering works, how the exhaust anchors were calibrated to v_flann.PNG, which civilian-traffic components were removed, which old Redd identifiers were eliminated, and the exact test commands/results.

The finished Seren should contain Flann as a first-class racer backed by v_flann.PNG, with correctly anchored smooth tailpipe boost exhaust, while the civilian NPC traffic subsystem and Redd identity are completely gone.
